import { StyleSheet } from "react-native";

export default StyleSheet.create({
  flatlist: {
    paddingBottom: 120,
    paddingHorizontal: 20,
  },
  flatlistContainer: {
    flexGrow: 1,
    height: "100%",
  },
});
